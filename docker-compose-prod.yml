version: '3.8' # 使用 3.8 版本的 compose 文件格式
services:
  frontend-prod: # 服务名称可以自定义
    image: frontend-prod # 使用与 docker build -t myimage . 相同的镜像
    build: 
      context: .
      dockerfile: Dockerfile.prod
    ports:
      - '3000:80'
    container_name: frontend-prod # 容器名称